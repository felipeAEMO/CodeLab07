CREATE TABLE usuarios (usuario_id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,login VARCHAR(140) NOT NULL, senha VARCHAR(255) NOT NULL, nome VARCHAR(255) NOT NULL, email VARCHAR(255) NOT NULL, ativo BOOLEAN DEFAULT TRUE NOT NULL);



